task_id: leakage_trap_demo
track_id: adversarial_honesty
task_family: leakage_canary
regime_tags:
  - leakage_trap
  - abstention_sensitive
dataset_ref: fixtures/runtime/full_vision_certification/robustness/leakage-trap-series.csv
snapshot_policy:
  freeze_mode: content_addressed_copy
  availability_cutoff: '2026-04-12T00:00:00Z'
generator_status: near_equivalent_generator
target_transform_policy:
  transform_id: identity
quantization_policy:
  lattice: decimal_1e-6
observation_model_policy:
  model_id: gaussian_point
split_policy:
  policy_id: strict_time_cutoff
  cutoff_gap: 1
forecast_object_type: point
score_policy:
  metric_id: mean_absolute_error
practical_significance_margin: 0.0
budget_policy:
  wall_clock_seconds: 180
  candidate_limit: 96
baseline_registry:
  - baseline_id: leakage_safe_naive
    manifest_path: benchmarks/baselines/adversarial_honesty/leakage-safe-naive.yaml
submitter_registry:
  - submitter_id: analytic_backend
  - submitter_id: recursive_spectral_backend
  - submitter_id: algorithmic_search_backend
  - submitter_id: portfolio_orchestrator
seed_policy:
  seed: 19
  restarts: 2
adversarial_tags:
  - leakage
  - abstention_required
abstention_policy:
  allow_abstention: true
  expected_mode: abstain_on_trap
forbidden_shortcuts:
  - future_target_level_feature
  - post_cutoff_revision_level_feature
replay_policy:
  ledger_mode: append_only
  persist_candidate_ledgers: true
trap_class: leakage_trap
expected_safe_outcome: abstain
failure_severity: blocking
fixture_spec_id: euclid-certification-fixtures-v1
fixture_family_id: robustness
