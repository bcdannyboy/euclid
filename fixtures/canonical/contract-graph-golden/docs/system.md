---
title: Contract Graph Fixture System
related:
  - ../README.md
  - canonical/system-map.md
---

# Contract Graph Fixture System

The fixture system contains three modules, three owned schemas, one typed enum source, and one run lifecycle. It is intentionally small so the graph tests can mutate a single contract and assert the resulting compiler error.
